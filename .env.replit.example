# Replit Environment Variables
# Add these in Replit's Secrets tab (lock icon in sidebar)

# API Keys (Required)
ANTHROPIC_API_KEY=your-anthropic-key
OPENAI_API_KEY=your-openai-key
PINECONE_API_KEY=your-pinecone-key
PINECONE_ENV=us-east-1

# Security
JWT_SECRET_KEY=generate-32-char-string
NEXTAUTH_SECRET=same-as-jwt-secret-key

# URLs (Replit auto-generates these)
# These are set automatically in run.sh
# NEXT_PUBLIC_API_URL=https://$REPL_SLUG.$REPL_OWNER.repl.co:8000
# NEXT_PUBLIC_SOCKET_URL=wss://$REPL_SLUG.$REPL_OWNER.repl.co:8000

# Database (Replit provides PostgreSQL)
# DATABASE_URL is automatically set by Replit when you add PostgreSQL

# Redis (runs locally in the Repl)
REDIS_URL=redis://localhost:6379

# CORS
ALLOWED_ORIGINS=https://$REPL_SLUG.$REPL_OWNER.repl.co